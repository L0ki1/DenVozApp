<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="app.lk.denunciacd">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileUser" />
        <activity
            android:name=".MenuDeslizante"
            android:label="@string/title_activity_menu_deslizante"
            android:theme="@style/AppTheme" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"
            tools:replace="android:value" />

        <activity
            android:name=".Reportar"
            android:label="@string/title_activity_reportar" />
        <activity android:name=".Splash" />
        <activity android:name=".Login_correo" />
        <activity android:name=".Registro_correo" />
        <activity android:name=".html" />
        <activity
            android:name=".Reportar_ubi"
            android:label="@string/title_activity_reportar_ubi"
            android:theme="@style/AppTheme" />
        <activity android:name=".InicioLogins">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tipos_reportes" />
        <activity android:name=".Tipos_reportes2"></activity>
    </application>

</manifest>